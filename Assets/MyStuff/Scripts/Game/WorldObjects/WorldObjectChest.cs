public class WorldObjectChest : WorldObject
{
    HudManager HudManager;

    //contents of chest


    public override void Activate(PlayerBrain pb)
    {
        //HudManager.Instance.
    }

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
